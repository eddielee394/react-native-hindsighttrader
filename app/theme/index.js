import { default as commonColor } from './variables/commonColor';
import { default as material } from './variables/material';
import { default as platform } from './variables/platform';
import { default as dark } from './variables/dark';

const theme = {
  commonColor,
  material,
  platform,
  dark,
};

export default theme;
